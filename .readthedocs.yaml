# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - python -m pip install -e . sphinx==8.2.3 sphinx-rtd-theme==3.0.2
sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true
